module.exports = {
	session_secret: process.env.SESSION_SECRET || 'owaspbrokenwebapps',
	access_token_expiry_seconds: Number(process.env.ACCESS_TOKEN_EXPIRY_SECONDS || 9),
	refresh_token_expiry_ms: Number(process.env.REFRESH_TOKEN_EXPIRY_MS || 604800000),
	refresh_token_cleanup_ms: Number(process.env.REFRESH_TOKEN_CLEANUP_MS || 3600000),
	refresh_token_bytes: Number(process.env.REFRESH_TOKEN_BYTES || 48)
};
